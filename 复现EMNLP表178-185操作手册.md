# 复现 `EMNLP_Constraint_Aware_RAG_Draft.md` 第178-185行数据（操作手册）

目标：复现下面这 6 行结果（分类别 CCR）：

- MiniLM (300): exclusion / negation / numeric
- Local Llama (60): exclusion / negation / numeric

---

## 0. 这 6 行数据本质上来自哪里

它们来自两份 JSON 报告：

1. `outputs/reports/rag_category_report_minilm_300.json`
2. `outputs/reports/rag_category_report_local_llama_60.json`

表格就是读取这两份报告里的 `by_category` 字段得到的。

---

## 1. 前置条件

在项目根目录执行（即 `/home/xingkun/ant`）：

```bash
cd /home/xingkun/ant
```

确认依赖已安装（如果没装就装）：

```bash
python3 -c "import sentence_transformers, datasets, numpy; print('ok')"
```

若报错：

```bash
pip install -r requirements.txt
```

---

## 2. 生成基准数据（300 queries）

这一步会产出 `constraint_benchmark_v1.jsonl`（100 negation + 100 exclusion + 100 numeric）：

```bash
python3 experiments/build_constraint_benchmark.py \
  --output-file data/processed/constraint_benchmark_v1.jsonl \
  --num-negation 100 \
  --num-exclusion 100 \
  --num-numeric 100 \
  --seed 42
```

---

## 3. 跑 MiniLM (300) 的分类别报告

```bash
CUDA_VISIBLE_DEVICES='' python3 experiments/rag_category_report.py \
  --eval-file data/processed/constraint_benchmark_v1.jsonl \
  --topic-model sentence-transformers/all-MiniLM-L6-v2 \
  --constraint-model outputs/checkpoints/constraint-encoder-v1 \
  --alpha 0.0 \
  --tau 0.6 \
  --top-k 3 \
  --report-file outputs/reports/rag_category_report_minilm_300.json
```

这一步对应表中的：

- `MiniLM (300) | exclusion`
- `MiniLM (300) | negation`
- `MiniLM (300) | numeric`

---

## 4. 跑 Local Llama (60) 的分类别报告

```bash
CUDA_VISIBLE_DEVICES='' python3 experiments/rag_category_report.py \
  --eval-file data/processed/constraint_benchmark_v1.jsonl \
  --topic-model /data/xingkun/local_model/Llama-3.2-3B-Instruct \
  --constraint-model outputs/checkpoints/constraint-encoder-v1 \
  --alpha 0.0 \
  --tau 0.6 \
  --top-k 3 \
  --max-queries 60 \
  --seed 42 \
  --report-file outputs/reports/rag_category_report_local_llama_60.json
```

这一步对应表中的：

- `Local Llama (60) | exclusion`
- `Local Llama (60) | negation`
- `Local Llama (60) | numeric`

---

## 5. 一键打印和论文里一致的 6 行

执行下面命令，会把两份报告拼成和论文同结构的 6 行：

```bash
python3 - <<'PY'
import json
from pathlib import Path

mini = json.loads(Path("outputs/reports/rag_category_report_minilm_300.json").read_text())
llama = json.loads(Path("outputs/reports/rag_category_report_local_llama_60.json").read_text())

print("| Setting | Category | Count | Vanilla CCR | Dual CCR | Improvement |")
print("| --- | --- | ---: | ---: | ---: | ---: |")

for row in mini["by_category"]:
    print(
        f"| MiniLM ({mini['num_queries']}) | {row['category']} | {row['count']} | "
        f"{row['vanilla_ccr']*100:.2f}% | {row['dual_ccr']*100:.2f}% | +{row['improvement']*100:.2f}% |"
    )

for row in llama["by_category"]:
    print(
        f"| Local Llama ({llama['num_queries']}) | {row['category']} | {row['count']} | "
        f"{row['vanilla_ccr']*100:.2f}% | {row['dual_ccr']*100:.2f}% | +{row['improvement']*100:.2f}% |"
    )
PY
```

---

## 6. 你应该看到的目标值（当前版本）

理论上会得到（顺序可能略有不同）：

- MiniLM (300)
  - exclusion: `54.33% -> 60.50%` (`+6.17%`)
  - negation: `22.00% -> 22.00%` (`+0.00%`)
  - numeric: `66.67% -> 89.33%` (`+22.67%`)

- Local Llama (60)
  - exclusion: `45.83% -> 60.42%` (`+14.58%`)
  - negation: `10.53% -> 21.05%` (`+10.53%`)
  - numeric: `66.67% -> 94.12%` (`+27.45%`)

---

## 7. 常见问题排查

1. **GPU OOM**
   - 用 CPU 运行：命令前加 `CUDA_VISIBLE_DEVICES=''`（上面已经加了）

2. **MiniLM 下载失败（网络问题）**
   - 如果本地缓存已有，通常可直接跑
   - 如果没有缓存，需要先联网下载一次

3. **结果和目标值有轻微差异**
   - 先确认参数完全一致：`alpha=0.0, tau=0.6, top-k=3`
   - Local Llama 一定要固定：`--max-queries 60 --seed 42`

---

## 8. 最终文件清单（复现后）

- `data/processed/constraint_benchmark_v1.jsonl`
- `outputs/reports/rag_category_report_minilm_300.json`
- `outputs/reports/rag_category_report_local_llama_60.json`

这三份就是第178-185行表格数据的直接来源。

